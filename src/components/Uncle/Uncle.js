import React from 'react';

const Uncle = ({ house }) => {
    return (
        <div>
            <p>Uncle</p>
            <p><small>{house}</small></p>
        </div>
    );
};

export default Uncle;